
apply plugin: "frege"

mainClassName = "com.github.mperry.frege.External"

tasks.compileFrege.dependsOn "compileJava"

compileFrege {
    verbose = true
    hints = true
    make = true
    inline = false
}

// can configure value like this, not used at the moment
frege {
    key1 = "value1"
}

